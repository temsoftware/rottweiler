<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<title>JR Corretora de Seguros</title>

  <%= stylesheet_link_tag 'jr' %>
  <%= javascript_include_tag :defaults %>

  <script>
    //Script para funcionar sub-menu de Produtos no IE
    startList = function()
    {
      if (document.all&&document.getElementById)
      {
        navRoot = document.getElementById("menu_esquerda_img_produto");

        navRoot.onmouseover=function()
        {
           this.className="menu_img over";//this.style.display = "block"; //className+=" over";
        }

        navRoot.onmouseout=function()
        {
          this.className="menu_img unover";
        }


        /*for (i=0; i<navRoot.childNodes.length; i++)
          {
            node = navRoot.childNodes[i];

            if (node.nodeName=="UL")
            {

              node.onmouseover=function()
              {
                this.style.display = "block";//this.className=" #menu_esquerda_img_produto.over"; //className+=" over";
              }

              node.onmouseout=function()
              {
                this.style.display = "none";//alert("BOM TD BEM!!!");//this.style.display = "none";//this.className=this.className.replace(" over", "");
              }
            }
          }*/
        }
      }
    window.onload=startList;
  </script>
</head>

<body>
	<div id="pag_fundo">
        <div id="topo">
            <img id="img_top" src="../images/topo.jpg">
        </div>
        <div id="conteudo">
          <%= render :partial => "layouts/menu_esquerda" %>
          <%= render :partial => "layouts/menu_seguradoras"%>
          <%= render :partial => "layouts/menu_parceiros"%>

          <%=yield%>

          <%=yield :insurances_box%>

        </div>
        <div id="rodape">

        </div>
	</div>
</body>
</html>

